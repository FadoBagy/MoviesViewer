@model IEnumerable<PopularMovieResultModule>

<h1>Movies</h1>

<h3>What's Popular</h3>
<div class="container text-center">
  <div class="row">
	@foreach (var movie in Model)
	{
		var posterPath = "https://image.tmdb.org/t/p/w500" + movie.PosterPath;
		@*<div class="col">
			<img src="@posterPath" width="200">
			<p><b>Title:</b> @movie.Title (@movie.ReleaseDate.ToString("dd-MM-yyyy"))</p>
			<p><b>Description:</b> @movie.Description</p>
		</div>*@

		<div class="card" style="width: 15rem;">
			<img src="@posterPath" class="card-img-top" alt="@movie.Title poster">
			<div class="card-body">
					<h5 class="card-title">@movie.Title</h5>
					<h6 style="opacity: 85%">@movie.ReleaseDate.ToString("MMM dd, yyyy")</h6>
				<p class="card-text">@movie.Description.Substring(0, 80)...</p>
			</div>
		</div>
	}
  </div>
</div>